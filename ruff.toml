line-length = 120
target-version = "py310"
extend-exclude = ["demo-repo", "dashboard/dist"]

[lint]
select = ["E", "F", "W", "I", "UP", "B", "SIM"]
ignore = [
    "E501",  # long lines allowed in the rule table / knowledge base
]

[format]
quote-style = "double"
